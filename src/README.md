# Source layout

The legacy project has no `src/` tree. Its active code is preserved in the capsule-root `desktop/`, `lib/`, `data/`, `prisma/`, `scripts/`, `types/`, and `__tests__/` directories to preserve its existing module aliases and Electron build layout.
